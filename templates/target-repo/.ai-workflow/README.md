# AI Workflow

This directory stores local AI workflow configuration and ignored run state.

Committed:

- `config.yml`
- this README

Ignored:

- `runs/`
- `cache/`
- `tmp/`

Promote only verified durable artifacts into tracked docs, such as
`docs/ai/verified-reports/`.
